import {SafeAreaView, Text} from 'react-native';

const SearchScreen: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Search</Text>
    </SafeAreaView>
  );
};

export default SearchScreen;
