import {SafeAreaView, Text} from 'react-native';

const LoginScreen: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Login</Text>
    </SafeAreaView>
  );
};

export default LoginScreen;
